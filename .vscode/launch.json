{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Ejecutar Programa C",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/${fileBasenameNoExtension}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "setupCommands": [
        {
          "description": "Habilitar la expansión de comandos de GDB",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "Compilar con GCC",
      "miDebuggerPath": "/usr/bin/gcc"
    }
  ]
}
